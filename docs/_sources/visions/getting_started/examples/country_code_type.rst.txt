Country code type example
=========================

The following example shows a way of defining a CountryCode type that tests if all values of the sequence are within a set of country codes as specified in ISO 3166-1 (alpha-2):

.. literalinclude:: ../../../../../examples/country_code_type_example.py
    :language: python
    :caption: country_code_type_example.py
    :name: CountryCode type example